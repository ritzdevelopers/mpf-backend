# Paste inside the server { } or location /api/ block that proxies to the Spring Boot API.
# Default client_max_body_size is 1m — large Excel + zip uploads get HTTP 413; the response
# usually has no Access-Control-Allow-* headers, so the browser reports a CORS error.
client_max_body_size 300m;
